<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
		  package="sinder.cse40333.sinderapp">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".LoginActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN"/>

				<category android:name="android.intent.category.LAUNCHER"/>
			</intent-filter>
		</activity>
		<activity android:name=".WelcomeScreen"></activity>
		<activity android:name=".Forms_V">
		</activity>
		<activity android:name=".SavedProjects_V">
		</activity>
		<activity android:name=".PastProjects_SM">
		</activity>
		<activity android:name=".AddProjects_SM">
		</activity>
	</application>

</manifest>